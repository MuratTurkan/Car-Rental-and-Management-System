import { Byte } from '@angular/compiler/src/util';
export interface UserDetailDto {
  userId: number;
  firstName: string;
  lastName: string;
  nationalityId: string;
  birthYear: number;
  email: string;
  status: string;
  claimName: string;
  claimId:number
}
